# NamaPerusahaan — Landing Page (Prototype)

Project kecil: landing page untuk layanan pembuatan website, pengadaan IT, dan SEO.

Cara menjalankan (lokal):

1. Buka terminal di folder project:

```bash
cd "c:/Users/Hype AMD/Downloads/valdoweb"
```

2. Jalankan web server sederhana (Python 3):

```bash
python -m http.server 8000
# lalu buka http://localhost:8000/ di browser
```

Catatan SEO & teknis singkat:
- Gunakan tag semantics (`h1`,`h2`,`article`) seperti di file untuk SEO on-page.
- Tambahkan meta description unik di tiap halaman.
- Tambahkan gambar optimasi dan `alt` attributes untuk aset yang sebenarnya.
- Sediakan sitemap.xml dan robots.txt saat deploy ke produksi.

Animasi: SVG blob di hero (CSS keyframes) dan efek reveal saat scroll.

Butuh bantuan selanjutnya? Saya bisa:
- Mengganti placeholder content dengan konten perusahaan anda.
- Menambahkan formulir backend untuk halaman kontak.
- Menghasilkan `sitemap.xml` dan schema lebih lengkap.
